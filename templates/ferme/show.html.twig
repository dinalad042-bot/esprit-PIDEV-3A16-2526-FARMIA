{% extends 'base.html.twig' %}

{% block title %}Ferme{% endblock %}

{% block body %}
    <h1>Ferme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ferme.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ferme.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ferme_index') }}">back to list</a>

    <a href="{{ path('app_ferme_edit', {'id': ferme.id}) }}">edit</a>

    {{ include('ferme/_delete_form.html.twig') }}
{% endblock %}
