{% extends 'layouts/admin.html.twig' %}

{% block title %}Dashboard Admin - FarmAI{% endblock %}

{% block body %}
<div class="welcome-section">
    <h1 class="welcome-title">Bienvenue, {{ app.user.prenom|default('aymen') }} {{ app.user.nom|default('aymen') }}!</h1>
    <p class="welcome-subtitle">Ne remettez jamais à demain ce qui peut être fait aujourd'hui ! <i class="fa-solid fa-seedling" style="color: var(--primary-green);"></i></p>
</div>

<div class="stats-grid">
    <!-- Green Card: Total -->
    <div class="stat-card green">
        <div class="stat-value">{{ stats.total|default(12) }}</div>
        <div class="stat-label">Total</div>
        <div class="stat-sublabel">Utilisateurs</div>
        <div class="stat-footer stat-footer-line">
            Objectif: 100%
        </div>
    </div>
    
    <!-- White Card: Admins -->
    <div class="stat-card">
        <div class="stat-value" style="color: var(--dark-bg);">{{ stats.admins|default(5) }}</div>
        <div class="stat-label" style="color: var(--dark-bg);">Admins</div>
        <div class="stat-sublabel">Gestionnaires</div>
        <div class="stat-footer stat-footer-line" style="color: #cbd5e1;">
            Actifs
        </div>
    </div>

    <!-- Light Gray Card: Experts -->
    <div class="stat-card light-gray">
        <div class="stat-value" style="color: var(--dark-bg);">{{ stats.experts|default(1) }}</div>
        <div class="stat-label" style="color: var(--dark-bg);">Experts</div>
        <div class="stat-sublabel">Consultants</div>
        <div class="stat-footer stat-footer-line" style="color: #cbd5e1;">
            Disponibles
        </div>
    </div>

    <!-- Dark Charcoal Card: Agricoles -->
    <div class="stat-card dark">
        <div class="stat-value">{{ stats.agricoles|default(5) }}</div>
        <div class="stat-label">Agricoles</div>
        <div class="stat-sublabel">Exploitants</div>
        <div class="stat-footer stat-footer-line">
            <i class="fa-solid fa-check" style="color: #4ade80;"></i> Vérifiés
        </div>
    </div>

    <!-- White Card: Fournisseurs -->
    <div class="stat-card">
        <div class="stat-value" style="color: var(--dark-bg);">{{ stats.fournisseurs|default(1) }}</div>
        <div class="stat-label" style="color: var(--text-muted);">Fournisseurs</div>
        <div class="stat-sublabel">Partenaires</div>
        <div class="stat-footer">
            <div class="footer-icon">
                <i class="fa-solid fa-gift" style="color: var(--dark-bg);"></i>
            </div>
        </div>
    </div>
</div>

<div class="bottom-grid">
    <!-- Actions Rapides -->
    <div class="action-card">
        <div class="action-header">
            <h3 class="action-title"><i class="fa-solid fa-bolt"></i> Actions Rapides</h3>
            <i class="fa-solid fa-ellipsis" style="color: #cbd5e1; cursor: pointer;"></i>
        </div>
        
        <div class="btn-group">
            <a href="{{ path('admin_users_index') }}?open=create" class="btn btn-dark">+ Nouvel Utilisateur</a>
            <button onclick="openStatsModal()" class="btn btn-outline-green" style="cursor: pointer;">Voir Statistiques</button>
        </div>
        
        <div class="action-list">
            <div class="list-item">
                <div class="list-icon new">NEW</div>
                <div class="list-text">Inscription récente</div>
            </div>
            <div class="list-item">
                <div class="list-icon refresh"><i class="fa-solid fa-arrows-rotate"></i></div>
                <div class="list-text">Mise à jour système</div>
            </div>
        </div>
    </div>

    <!-- FarmAI Enterprise -->
    <div class="enterprise-card">
        <h3 class="enterprise-title"><i class="fa-solid fa-building"></i> FarmAI Enterprise</h3>
        <p class="enterprise-text">
            Gérez votre exploitation agricole intelligente avec les dernières technologies d'IA.
        </p>
        <i class="fa-solid fa-seedling enterprise-icon"></i>
    </div>
</div>

{% include 'admin/statistics/_statistics_modal.html.twig' %}

{% endblock %}
